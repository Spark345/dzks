import classes from "./Header.module.css"
// import logo from  "../../assetc/images/logo.svg"

const Header = (props) =>{
    return(
        <div className={classes.header}>

        </div>
    );
}

export default Header;